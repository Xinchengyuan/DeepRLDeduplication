Prerequisite: Python 3.0+

Package Required:

-NumPy
-Tensorflow v.2.0+
-Keras
-progressbar

After installing pip3, install packages using command:
    pip3 install (package name)

You also need:
- gym (pip3 install gym)

After installing gym, cd to DeepRLDeduplication/code/gym-dedup/, then run "pip3 install -e ." (do not forget the period(.) ) to install the environment that the RL agent will be interacting with.

This project also need the hf-stat tool to run Download the tool at: https://tracer.filesystems.org/fs-hasher-0.9.5.tar.gz
